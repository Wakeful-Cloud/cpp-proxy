{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build (Eigen)",
			"type": "shell",
			"command": "task build:program && task build:proxy",
			"options": {
				"cwd": "${workspaceFolder}/prototypes/eigen",
				"env": {
					"VERBOSE": "true"
				}
			},
			"problemMatcher": []
		}
	]
}